[project]
name = "pdfrag"
version = "0.1.0"
description = "Retrieval-Augmented Generation system over PDF/DOCX with FastAPI, ChromaDB, and Azure OpenAI."
readme = "README.md"
requires-python = ">=3.10"
dependencies = []
